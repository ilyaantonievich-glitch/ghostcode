{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "bun install --frozen-lockfile && bun run build",
    "buildpacks": ["node"]
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "bun run start:chat",
    "environment": [
      {
        "key": "NODE_ENV",
        "value": "production"
      }
    ]
  }
}